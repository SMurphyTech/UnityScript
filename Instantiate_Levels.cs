using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instantiate_Levels : MonoBehaviour
{
    public int flag;

    // Start is called before the first frame update
    void Start()
    {
        flag = 0;
    }

    void nextLevel()
    {
        if(flag == 0)
        {

        }
    }
}
